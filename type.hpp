#ifndef BUFFER_HANDLE_HTTP_HEADER_TYPE_HPP
#define BUFFER_HANDLE_HTTP_HEADER_TYPE_HPP

#include <buffer_handle/action.hpp> // action
#include <buffer_handle/align.hpp> // align
#include <buffer_handle/case.hpp> // case
#include <buffer_handle/config.hpp> // config

namespace buffer_handle_http_header
{
  typedef buffer_handle::action action;
  typedef buffer_handle::align align;
  typedef buffer_handle::case_ case_;
  typedef buffer_handle::config config;
};

#endif/*BUFFER_HANDLE_HTTP_HEADER_TYPE_HPP*/
